<ul>
  <% available_users.teams.where.not(id: current_team).find_each do |team| %>

    <li>
      <%= link_to team.name, login_into_team_url(subdomain: team.subdomain), target: "_blank" %>
    </li>

  <% end %>
</ul>
